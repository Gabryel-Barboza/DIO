import chalk from 'chalk';
import logSymbols from 'log-symbols';
// Pacotes e dependências

console.log(chalk.blue.bgGreen.bold('hello, Chalk!'));

console.log(logSymbols.success, chalk.blue.bold('Operação realizada com sucesso!'));
